/* eslint-disable @typescript-eslint/no-inferrable-types */
const origin: boolean = true
const credentials: boolean = true
const cors = { origin, credentials }

export { origin, credentials }
export default cors
